# Server port for the BFF service
server.port=8080

# Downstream Microservice URLs
service.user.url=http://localhost:8081
service.account.url=http://localhost:8082
service.transaction.url=http://localhost:8083

# Kafka Producer Configuration
spring.kafka.producer.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Logging Topic
logging.kafka.topic=vbank-logs